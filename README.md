# Churn Prediction

Machine-learning project based on the supplied MLOPs notebook for customer churn prediction.

## Run

```bash
pip install -r requirements.txt
python pipelines/run_baseline.py
```

The pipeline performs the notebook preprocessing, trains a Random Forest classifier, and produces evaluation artifacts.
